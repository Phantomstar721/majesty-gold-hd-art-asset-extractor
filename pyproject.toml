[project]
name = "majesty-gold-hd-art-asset-extractor"
version = "0.2.0"
description = "Local-only Majesty Gold HD art asset extractor and organizer."
requires-python = ">=3.9"
dependencies = [
    "Pillow>=10",
    "imageio-ffmpeg>=0.6",
]

[tool.setuptools]
py-modules = []
