[project]
name = "majesty-gold-hd-asset-spelunker"
version = "0.1.0"
description = "Local-only Majesty Gold HD art asset extractor and organizer."
requires-python = ">=3.9"
dependencies = [
    "Pillow>=10",
]

[tool.setuptools]
py-modules = []
